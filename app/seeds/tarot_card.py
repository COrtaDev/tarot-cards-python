class TarotCard:
    def __init__(self, name, suit, img, api_endpoint, deck):
        self.name = name
        self.suit = suit
        self.img = img
        self.api_endpoint = api_endpoint
        self.deck = deck
